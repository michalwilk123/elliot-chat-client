


def test_send_encrypted_message(mocker):
    """Implementing test: 
    tests/chat/crypto/test_ratchets.py::
        test_send_recieve_alice_first
    But this time by actually send message from mocked server
    """
    assert False

def test_recv_encrypted_message(mocker):
    """Implementing test: 
    tests/chat/crypto/test_ratchets.py::
        test_send_recieve_alice_first
    But this time by actually recieve message 
    from mocked server
    """
    assert False

def test_short_conversation(mocker):
    """
    Same test as the ones above but now we 
    combine them together
    """
    assert False

def test_should_raise_when_recv_bad_key(mocker):
    assert False

def test_full_user_creation(mocker):
    """
    Test full user creation handshake
    with mocked database and server
    """
    assert False
